{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "cd artifacts/cinematic-gen && npm install --legacy-peer-deps && npm run build"
  },
  "devDependencies": {
    "typescript": "~5.9.2"
  }
}
