services:
  - type: web
    name: directors-cut-v2
    runtime: static
    buildCommand: bash build.sh
    staticPublishPath: dist
    pullRequestPreviewsEnabled: false
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
